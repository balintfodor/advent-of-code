def read_many_lines_input(input_str, n_blocks, block_delim):
    # split_index = str_list.index("")
    # blocks = input.split(block_delim)
    pass

def read_matrix(input_str):
    return [list(map(int, line.split())) for line in input_str.strip().split("\n")]
